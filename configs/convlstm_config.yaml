# ConvLSTM model configuration
model:
  name: "convlstm"
  input_dim: 8
  hidden_dim_list: [16, 16, 8]
  kernel_size_list: [[3, 3], [3, 3], [3, 3]]
  num_layers: 3
  output_dim: 8
  T_out: 1
  dropout: 0.2

# Training configuration
training:
  epochs: 100
  batch_size: 4
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 10
  save_every: 5
  device: "cuda"
  seed: 42

# Optimizer configuration
optimizer:
  name: "adam"
  momentum: 0.9

# Scheduler configuration
scheduler:
  name: "step"
  step_size: 30
  gamma: 0.1
  T_max: 200
  patience: 10

# Data configuration
data:
  file_path: "/home/zxh/CQ/dataset/isoprene_results.nc" # 更改为数据文件路径
  var_name: ["CHLA", "PAR", "SST", "sla", "tco", "HCHO", "windspeed", "isoprene"]
  input_seq_len: 8
  target_seq_len: 1
  train_ratio: 0.7
  val_ratio: 0.2
  test_ratio: 0.1
  shuffle: true
  num_workers: 4

# Logging configuration
logging:
  log_dir: "logs"
  log_level: "INFO"
  save_logs: true
